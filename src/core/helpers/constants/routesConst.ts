// eslint-disable-next-line no-shadow
enum routesConst {
  LOGIN = '/login',
  REGISTER = '/register',
  HOME = '/',
  PROFILE = '/profile/:uderId?',
  EDITOR = '/editor',
  ANY = '*',
}

export default routesConst;
