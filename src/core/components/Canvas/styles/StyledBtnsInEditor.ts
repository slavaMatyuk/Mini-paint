import styled from 'styled-components';

const StyledBtnsInEditor = styled.div`
  position: absolute;
  top: 55px;
  left: 5px;
  & button {
    width: 100%;
  }
`;

export default StyledBtnsInEditor;
